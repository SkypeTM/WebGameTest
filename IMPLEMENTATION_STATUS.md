# 명세 검토와 구현 범위

기준: `Codex_Game_Spec.docx` 명세 1.1, `START_PROMPT_KO.md`, 원본 JSON 2개. 목표는 사용자 요청의 첫 플레이 버전입니다. 2~5단계 전체 완료로 간주하지 않습니다.

## 적용한 초기값과 결정

- 초기 파티 AR1~AR4, 은화 100, 인물 HP 60. 명세에 확정되지 않은 시작 자원을 개발 초기값으로 추가했습니다.
- 파티 1~4명, 캐릭터당 4장, 자원 4, 손패 6, 상한 10. 비보존 손패는 턴 종료 시 버리고 드로 덱이 비면 시드 기반으로 재섞습니다.
- 일반/정예/보스 HP 30/70/180, 기본 공격 6/12/18에서 특수 행동을 적용했습니다. 인원 난이도 보정은 아직 없으므로 첫 플레이는 4인 권장입니다.
- 관계 쌍은 명세 그대로 사용하며 중복 쌍을 한 번만 계산합니다. 같은 팩션 2/3/4명 방어 5/8/10%, 우호 보호막 5(최대 10), 대척 스트레스 2(최대 4)입니다.
- 원본 캐릭터 32명과 몬스터 56종 JSON을 수정하지 않았습니다. 여성 30, 남성 2, 전원 성인, 7맵×8종, 기계군 24종을 테스트합니다.
- 첫 맵의 실제 전투 8종만 구현했습니다. 타 맵 적을 같은 수치와 외형만 바꿔 구현 완료로 표시하지 않습니다.
- 인증 라이브러리는 Better Auth를 채택했습니다. 외부 인증/DB 자격 증명 없이 실행 가능한 로컬 서버 SQLite를 사용합니다. 권장 PostgreSQL을 대체한 개발용 선택이며 PostgreSQL 어댑터를 구현했다고 주장하지 않습니다.
- 공용 시장이 없으므로 시장 거래 경합·소유권 이전은 아직 구현하지 않았습니다. 행동 경합 처리는 실제 구현·검증했습니다.
- 초기 장비 2종은 각 캐릭터의 기본 키트입니다. 개별 인스턴스 창고/거래형 장비로 완성한 것이 아닙니다.
- 32명 영입은 기본 비용으로 열어 두었습니다. 고유 카드·팩션 조건·전용 모집 퀘스트는 후속 단계입니다. 원본 고유 카드 설명은 모집의 인물 기록에 유지합니다.
- 부상은 저장/표시만 하며 치료 비용·능력 페널티는 후속 단계입니다. 스트레스 누적과 회복은 동작하지만 정신 붕괴 등 고급 효과는 미구현입니다.
- 최종 그림이 없으므로 공통 SVG 실루엣을 명시적으로 임시 사용합니다. 인물별 외형을 완성했다고 보지 않습니다. ID별 경로 교체가 가능합니다.

## 전체 요구사항 대응

| 제작 단계 | 상태 | 남은 일 |
| --- | --- | --- |
| 1 기반과 한 판 | 완료 | 고유 카드·장비 인스턴스 확장은 후속 밸런스 작업 |
| 2 성장과 관계 | 기초 완료 | 진화·룬·진급·복합 우호도 의뢰는 후속 작업 |
| 3 실제 유저 거래 | 기초 완료 | 재료 매물의 등록·구매·취소·만료, 거래 내역, 5% 수수료와 원자 이전 구현. 완전한 에스크로는 후속 작업 |
| 4 전체 콘텐츠 | 경로·엔딩 기반 완료 | 7맵 56종 전투 경로, 엔딩 선택·환생 기반 구현. 48종 고유 행동, 32명 고유 카드, 16개 후일담 조합은 후속 작업 |
| 5 화면과 자산 | 반응형 QA 기반 완료 | 88개 개별 완성 이미지, 연출·애니메이션·음향, 실제 모바일 하드웨어 QA는 남음 |

## 후속 단계에서 지켜야 할 규칙

- 배타적 4쌍: 왕국/공화연맹, 조합/결사, 교단/성회, 수호회/해방전선. 전달·우호도 +25/-30·반대 퀘스트 포기를 하나의 트랜잭션으로 확정하고 우호도 회복으로 되돌리지 않습니다.
- 모든 16개 엔딩 조합을 접근 가능하게 하고, 랜덤 영입이나 적대 상태 때문에 필수 스토리가 막히지 않도록 대체 담당자/복구 의뢰가 필요합니다.
- 플리마켓에는 실제 유저 소유 물품만 등록합니다. NPC 가짜 거래로 표시하지 않습니다. 장착품·퀘스트품·희생 보상은 거래 제한합니다.
- 암시장은 대가·효과·기간·해제 조건·대상을 사전 표시하고, 희생은 필수가 아닙니다.
- 환생은 현재 루트 필수 의뢰와 엔딩 완료 후 선택합니다. 계승 3개(개별 수량), 퀘스트/희생품 제외, 강화/룬 초기화, 모든 성장·우호도·분기·명부·시설·화폐 초기화, 기본 파티 지급, 엔딩/업적은 표시 기록만 유지합니다. 미정산 시장 거래를 먼저 해결해야 합니다.
- 미구현 시스템의 테스트를 통과한 것처럼 보고하지 않습니다. 시장 동시 구매, 암시장 원자 교환, 배타 퀘스트, 엔딩 조건, 환생 취소/실행 테스트는 각 단계 구현 시 추가해야 합니다.

## 외부 설정 필요

실서비스 PostgreSQL과 마이그레이션, 지속 스토리지/백업, 배포 대상과 HTTPS 도메인, 운영 인증 비밀값, 메일 서비스(인증/비밀번호 재설정), 공용 시장 운영 정책. 현재는 외부 서비스에 배포하거나 결제하지 않았습니다.
