# 검증 기록

2026-09-17 KST. Node 24.19.0, Next.js 16.3.5, React 19.3.0, Better Auth 1.7.5, SQLite(node:sqlite), Microsoft Edge + Playwright 1.63.0.

## 결과

- TypeScript 타입 검사 통과.
- Next.js 운영 빌드 통과: `/`, `/api/auth/[...all]`, `/api/game`.
- 규칙·저장 자동 테스트 18개 통과.
- 브라우저 통합 테스트 3개 통과. 테스트 전용 SQLite와 포트 3100 서버를 자동 생성하는 격리 환경에서 실행.
- 1440×1000 PC와 390×844 모바일 터치 컨텍스트에서 화면 캡처 확인. 가로 넘침 없음, 페이지 JavaScript 오류 없음.

## 확인한 기능

1. 실제 계정 가입/로그인, 최대 4명 편성 해제·추가, 캐릭터 장비 변경.
2. 던전 진입, 전투 손패 6/자원 4, 카드 선택 후 대상 클릭, 턴 종료.
3. 전투 중 새로고침 전후 전체 JSON 동일: 손패·덱 순서·턴·HP·적 상태 포함.
4. 별도 모바일 로그인 세션으로 동일 전투 복원, 기존 기기 요청 423 거절.
5. 브라우저 네트워크 오프라인 → 입력 잠금 → 같은 요청 재전송 → 턴 1회 증가.
6. 첫 전투 → 보상 획득 → 휴식 → 안뜰 전투 → 보급 → 성탑 파괴와 보스 처치 → 보상 → 귀환.
7. 귀환 뒤 은화/재료 확정, 로그아웃·재로그인 후 전체 게임 상태 동일.
8. 같은 요청을 동시에 2번 보내도 동일 응답/효과 1회, 오래된 버전은 409 거절, 다른 내용으로 요청 ID 재사용 금지.
9. 허가되지 않은 출처 403, 인증 없는 상태 조회 401, 계정별 저장 분리, 실패 행동 롤백.
10. 파티 인원·중복·소유권 검증, 보상 반복 수령 차단, 다른 방으로 잘못 이동 차단, 전멸 시 미확보 전리품 손실.
11. 순수 상태 전이 재현성, 전투 불능 인물 카드 제한, 퍼즐 정답/오답, 던전 경험치, 관계 효과 상한.
12. 원본 32명/30여/2남/성인, 몬스터 56고유/7맵×8/기계군24 검사.
13. 동일 카드 재전송 시 자원/피해 1회 적용, 별도로 다시 연 SQLite 연결에서 서버 상태 동일.
14. 공격 가로막기 시 실제 피격자의 표식 적용, 수호 스킬로 보호한 다른 아군의 반격 적용.
15. 거점 시설 강화, 동료 훈련·치료, 업적 기록과 비용 검증.
16. 두 계정의 재료 매물 등록·구매와 동일 요청 재전송을 포함한 원자 거래.
17. 7개 맵 경로, 최종 엔딩 선택, 최대 3개 계승품 환생.
18. 시장 매물 24시간 만료, 재료 자동 회수, 5% 판매 수수료, 거래 내역 기록.
19. 구버전 저장 상태 정규화와 최대 전투 40턴 제한.
20. 카드별 SVG 아트와 캐릭터·몬스터 공격·피격·방어·회복 모션 연결.

브라우저 테스트는 `tests/browser/flow.spec.ts`, 규칙/저장 검증은 `tests/game.test.ts`와 `tests/store.test.ts`에서 재실행할 수 있습니다. Playwright가 `scripts/e2e-server.ts`를 통해 테스트 DB와 서버를 자동으로 격리하므로 공유 개발 서버 충돌을 피합니다. 생성 캡처와 HTML 보고서는 git에서 제외되는 `test-results`, `playwright-report`에 있습니다.

## 미검증·미구현

실제 휴대폰 하드웨어·Safari·Firefox, 다중 서버 배포 부하, 운영 인증/메일/백업은 검증하지 않았습니다. 시장 만료·수수료·거래 내역, 배타 퀘스트·16개 후일담 조합, 48종 몬스터 고유 행동과 32명 고유 카드는 후속 구현이 필요합니다. 임시 공통 실루엣은 최종 아트 검수가 아닙니다.
